import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  layout: {
    flexGrow: 1,
    flexShrink: 1,
  },
  layoutWrapper: {
    alignSelf: 'center',
    flexGrow: 1,
    flexShrink: 1,
    maxWidth: 541 + 16 * 2,
    width: '100%',
  },
});
